<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="search">
    
    <resultMap id="staffVOMap" type="erp.com.a.model.StaffVO">
		<result column="staff_no" property="staffNo" />
		<result column="staff_name" property="staffName" />
		<result column="jumin_no" property="juminNo" />
		<result column="school_code" property="schoolCode" />
		<result column="department_code" property="departmentCode" />
		<result column="graduate_day" property="graduateDay" />
		<result column="gender" property="gender" />
	</resultMap>
	
    
    <!-- 모두 검색 -->
    <select id="selectAll" resultMap="staffVOMap">
        SELECT *
        FROM STAFF
    </select>
    
    <!-- staff 테이블에 추가 -->
    <insert id="insertStaff" parameterType="erp.com.a.model.StaffVO">
        INSERT INTO STAFF(STAFF_NO, STAFF_NAME, JUMIN_NO, SCHOOL_CODE, DEPARTMENT_CODE, GENDER, GRADUATE_DAY)
        VALUES(staff_seq.nextval, #{staffName}, #{juminNo}, #{schoolCode}, #{departmentCode}, #{gender}, #{graduateDay})
    </insert>
    
    
    <!-- staff_skill 테이블에 추가 -->
    <insert id="insertStaffSkill" parameterType="erp.com.a.model.StaffSkillVO">
        INSERT INTO STAFF_SKILL ( STAFF_SKILL_NO, STAFF_NO, SKILL_CODE )
        VALUES( staff_skill_seq.nextval, #{staffNo}, #{skillCode} )
    </insert>
    
</mapper>





